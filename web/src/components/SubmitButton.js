import { Button } from '@mui/material';

const SubmitButton = () => {
	return (
		<Button
			type='submit'
			variant='outlined'
			fullWidth
			color='inherit'
		>
			Enviar
		</Button>
	);
};

export default SubmitButton;
